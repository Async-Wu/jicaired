<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/jingcaimainlayout"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <include layout="@layout/title_actionbar" >
    </include>

<!--    <include
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/ffootballtopbarLayout"
        layout="@layout/new_activity_loading" >
    </include>

    <include
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/ffootballtopbarLayout"
        layout="@layout/new_activity_failed"
        android:visibility="gone" >
    </include>  -->

<TextView 
    android:id="@+id/bascketball_layout_no_game"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:text="此玩法目前没有比赛"
    android:textSize="@dimen/text_size_m"
    android:textColor="@color/grey_new"
    android:gravity="center"
    android:visibility="gone"
    />
    <FrameLayout
        android:id="@+id/basketball_mainlayout"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" >

   
    </FrameLayout>

</LinearLayout>